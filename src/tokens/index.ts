import colors from './colors'
import defaults from './default'
import fontSizes from './font-sizes'
import scheme from './scheme'

export const gwindTokens = {
    colors,
    defaults,
    fontSizes,
    scheme,
}

export { colors, defaults, fontSizes, scheme }