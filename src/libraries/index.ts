import theme from './theme'
import gwindColors from './colors'
import gwindDefault from './default'
import gwindFontSizes from './font-sizes'
import gwindScheme from './scheme'


export const gwindThemeConfig = {
    colors: gwindColors,
    default: gwindDefault,
    fontSizes: gwindFontSizes,
    scheme: gwindScheme,
}
export default theme